{
  "name": "catfacts",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/user-event": "^7.2.1",
    "apollo-boost": "^0.4.7",
    "axios": "^0.19.2",
    "react": "^16.12.0",
    "react-apollo": "^3.0.0-beta.2",
    "react-dom": "^16.12.0",
    "react-hookstore": "^1.4.3",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.3.1",
    "semantic-ui-react": "^0.88.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -r build && react-scripts build && rm -r ../build && cp -r build ../build",
    "test": "jest --verbose",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.4.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "proxy": "http://localhost:3001"
}
